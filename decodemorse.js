decodeMorse = function(morseCode){
    let morseWords = morseCode.split("   ");
    
    // for each morseWord index
    morseWords.forEach()
    // create a morseLetters array
    
    // return a decoded word to a solution array 
    // join decoded words into a string (with spaces between!)
  }

console.log(decodeMorse('.... . -.--   .--- ..- -.. .'));